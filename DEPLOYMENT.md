# 投票系统部署指南

## 系统要求

- Ubuntu 24.04 LTS
- 至少 2GB RAM
- 至少 20GB 磁盘空间
- 互联网连接

## 部署步骤

### 1. 准备环境

将部署脚本上传到服务器，然后执行：

```bash
# 上传文件到服务器
# 例如：scp deploy.sh start.sh ubuntu@your-server-ip:~

# 登录服务器
# ssh ubuntu@your-server-ip

# 给脚本添加执行权限
chmod +x deploy.sh start.sh

# 执行部署脚本
./deploy.sh
```

### 2. 配置项目

部署脚本执行完成后，需要：

1. **修改 git 仓库地址**：在 `deploy.sh` 中替换 `<your-repo-url>` 为实际的项目仓库地址
2. **配置环境变量**：在 `server/.env` 文件中配置必要的环境变量

### 3. 启动系统

```bash
./start.sh
```

## 服务管理

### 查看服务状态

```bash
pm2 status
```

### 查看服务日志

```bash
pm2 logs
```

### 停止服务

```bash
pm2 stop all
```

### 重启服务

```bash
./start.sh
```

## 访问地址

- **前端应用**：http://localhost:4173
- **后端 API**：http://localhost:3001

## 注意事项

1. **文件上传功能**：由于涉及文件系统操作，需要确保 `server/uploads` 目录存在且具有写入权限
2. **数据库**：系统使用 SQLite 数据库，数据存储在 `server/database.sqlite` 文件中
3. **端口配置**：默认使用 3001（后端）和 4173（前端）端口，如需修改请更新配置文件
4. **安全性**：生产环境部署时，建议配置反向代理和 SSL 证书

## 故障排查

### 常见问题

1. **端口被占用**：检查端口使用情况并修改配置
2. **依赖安装失败**：确保网络连接正常，尝试重新执行部署脚本
3. **数据库连接失败**：检查 SQLite 文件权限和路径配置
4. **服务启动失败**：查看日志文件了解具体错误信息

### 日志位置

- **后端日志**：`server/` 目录下的控制台输出
- **前端日志**：构建和运行时的控制台输出

## 系统架构

- **前端**：Vue 3 + TypeScript + Ant Design Vue
- **后端**：Express.js + Sequelize ORM + SQLite
- **数据存储**：SQLite 数据库
- **进程管理**：pm2

## 功能模块

1. **用户认证**：工号登录，支持 root 管理员账户
2. **投票功能**：支持多个奖项的投票
3. **结果统计**：实时统计投票结果和排名
4. **Excel 导入**：支持批量导入用户
5. **数据清洗**：支持清空投票数据和用户数据
6. **数据库同步**：支持与远程数据库同步

## 维护建议

1. **定期备份**：备份 SQLite 数据库文件
2. **日志监控**：定期查看服务日志，及时发现问题
3. **依赖更新**：定期更新项目依赖，确保安全性
4. **性能优化**：根据实际使用情况调整服务器配置

---

**部署完成后，系统即可正常使用。如有其他问题，请参考项目文档或联系技术支持。**