# 侧边栏外观调整

## 已完成的修改

### 1. 关闭侧边栏折叠功能
- 修改了 `HomeView.vue` 文件中的 `a-layout-sider` 组件
- 将 `collapsible` 属性设置为 `"false"`
- 移除了 `collapsed-width` 属性配置

### 2. 调整侧边栏色调为亮色
- 修改了 `a-menu` 组件的主题设置
- 将 `:theme` 属性从 `'dark'` 改为 `'light'`
- 确保图标在亮色背景下更加可见

## 预期效果

- 侧边栏将保持展开状态，不再自动折叠
- 侧边栏背景变为亮色，图标更加清晰可见
- 菜单文字和图标对比度提高，改善可读性
- 整体视觉效果更加现代化和清爽

## 后续步骤

- 测试侧边栏的显示效果
- 验证图标是否清晰可见
- 确认所有菜单功能正常工作
- 根据需要进一步调整样式